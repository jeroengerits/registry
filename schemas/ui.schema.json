{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeroengerits/registry/schemas/ui.schema.json",
  "title": "UI component manifest",
  "description": "The schema for the root ui.json file in a component repository.",
  "examples": [
    {
      "schemaVersion": 1,
      "name": "example-component",
      "description": "Example registry component fixture for testing manifest versioning.",
      "files": [],
      "dependencies": {},
      "components": []
    }
  ],
  "type": "object",
  "required": ["schemaVersion", "name", "files", "dependencies", "components"],
  "properties": {
    "schemaVersion": { "const": 1 },
    "name": { "type": "string", "pattern": "^[a-z][a-z0-9]*(?:-[a-z0-9]+)*$" },
    "description": { "type": "string", "minLength": 1, "pattern": ".*\\S.*" },
    "files": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["source", "target"],
        "properties": {
          "source": { "$ref": "#/$defs/relativePath" },
          "target": { "$ref": "#/$defs/relativePath" }
        },
        "additionalProperties": false
      }
    },
    "dependencies": {
      "type": "object",
      "propertyNames": { "$ref": "#/$defs/nonBlankString" },
      "additionalProperties": { "$ref": "#/$defs/nonBlankString" }
    },
    "components": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["repository"],
        "properties": {
          "repository": { "$ref": "#/$defs/nonBlankString" },
          "version": { "$ref": "#/$defs/nonBlankString" }
        },
        "additionalProperties": false
      }
    }
  },
  "$defs": {
    "nonBlankString": {
      "type": "string",
      "minLength": 1,
      "pattern": ".*\\S.*"
    },
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?![A-Za-z]:[\\\\/])(?!.*(?:^|[/\\\\])\\.\\.(?:[/\\\\]|$)).*\\S.*$"
    }
  },
  "additionalProperties": false
}
